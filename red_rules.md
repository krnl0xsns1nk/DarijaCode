# here is the strict rules of building this language

1. it should be compiled to binary
1. independent (without need any other language to use it)
1. everything the user sees must be in `Darija` as possible including "error report, lang keywords, etc..."
1. it should have its own ecosystem
